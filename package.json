{
  "name": "opera-italian-reader",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/sync-content.mjs",
    "dev": "vite",
    "prebuild": "node scripts/sync-content.mjs",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "sync-content": "node scripts/sync-content.mjs",
    "tokenize-parity": "node scripts/tokenize-parity.mjs",
    "test:unit": "node --test src/shared/*.test.ts"
  },
  "dependencies": {
    "preact": "^10.29.7"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@types/node": "^24.13.3",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  }
}
